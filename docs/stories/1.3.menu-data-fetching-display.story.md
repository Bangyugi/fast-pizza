# Story 1.3: Menu Data Fetching & Display

## Status

Done

## Story

**As a** Customer,
**I want** to view the full list of available pizzas fetched from the menu,
**so that** I can decide what I want to order.

## Acceptance Criteria

1. A service function is created to fetch menu data from the "Fast React Pizza Co." API.
2. A new route (`/menu`) is created and linked from the home page.
3. The `/menu` route uses a React Router `loader` to fetch the menu data before the page renders.
4. The `Menu` component successfully receives and displays a list of `MenuItem` components, one for each pizza.
5. If the API fetch fails, a user-friendly error message is displayed on the page instead of the menu.

## Tasks / Subtasks

- [x] Task 1: Enhance `src/services/apiRestaurant.js` for menu fetching (AC: 1)

  - [x] Subtask 1.1: Ensure the `getMenu` function correctly fetches data from `https://react-fast-pizza-api.jonas.io/api/menu`.
  - [x] Subtask 1.2: Add error handling for API calls within `getMenu`.

- [ ] Task 2: Create/Update `src/features/Menu/Menu.jsx` (AC: 4)

  - [x] Subtask 2.1: Implement the `Menu` component to receive menu data (e.g., via `useLoaderData` from React Router).
  - [x] Subtask 2.2: Render a list of `MenuItem` components, one for each pizza in the menu data.
  - [x] Subtask 2.3: Implement basic loading state display.
  - [x] Subtask 2.4: Implement error state display if menu data fetching fails.

- [x] Task 3: Create/Update `src/features/Menu/MenuItem.jsx` (AC: 4)

  - [x] Subtask 3.1: Implement the `MenuItem` component to display individual pizza details. (Details for individual item display will be further specified in Story 1.4).

- [x] Task 4: Configure Routing in `src/App.jsx` for `/menu` route (AC: 2, 3, 5)

  - [x] Subtask 4.1: Add a new route for `/menu` that renders the `Menu` component.
  - [x] Subtask 4.2: Implement a React Router `loader` function for the `/menu` route that calls the `getMenu` function from `apiRestaurant.js`.
  - [x] Subtask 4.3: Handle potential errors from the `loader` function to display a user-friendly error message.

- [x] Task 5: Link to `/menu` from the home page or header (AC: 2)

  - [x] Subtask 5.1: Add a navigation link to `/menu` from an appropriate location (e.g., header, home page).

- [x] Task 6: Unit Tests (AC: All)
  - [x] Subtask 6.1: Given no test runner is configured, conduct manual verification.

## Dev Notes

### Previous Story Insights

Story 1.2 set up the basic application layout and routing. The `App.jsx` was modified to include the router configuration, and `AppLayout.jsx` was created. This story builds upon that routing infrastructure.

### Data Models

No client-side data models. Data structures are implicitly defined by the responses from the Fast React Pizza API.
[Source: docs/brownfield-architecture/data-models-and-apis.md#data-models]

### API Specifications

- **Fast React Pizza API**:
  - Endpoint: `https://react-fast-pizza-api.jonas.io/api`
  - Usage: Fetching menu.
  - Implementation: `src/services/apiRestaurant.js` already exists and contains functions to get menu/order data. The function `getMenu()` in `apiRestaurant.js` should be used.
  - [Source: docs/brownfield-architecture/data-models-and-apis.md#api-specifications]
  - [Source: docs/brownfield-architecture/integration-points-and-external-dependencies.md#external-services]
  - [Source: docs/brownfield-architecture/source-tree-and-module-organization.md#key-modules-and-their-purpose]

### Component Specifications

- `src/features/Menu/Menu.jsx`: This component will be the main view for the `/menu` route, responsible for rendering a list of `MenuItem` components.
- `src/features/Menu/MenuItem.jsx`: This component will display the details of an individual pizza item.
- [Source: docs/brownfield-architecture/source-tree-and-module-organization.md#key-modules-and-their-purpose]

### File Locations

- `src/services/apiRestaurant.js`: Utilized for fetching menu data.
- `src/features/Menu/Menu.jsx`: New or updated component for the menu page.
- `src/features/Menu/MenuItem.jsx`: New or updated component for individual menu items.
- `src/App.jsx`: Update routing configuration to include `/menu` route and loader.
- [Source: docs/brownfield-architecture/source-tree-and-module-organization.md#key-modules-and-their-purpose]

### Technical Constraints

- Use React Router `loader` function for data fetching in the `/menu` route.
- Error handling for API fetch failures should result in a user-friendly message.

### Testing

- **Test file location**: No guidance available in architecture docs.
- **Test standards**: No guidance available in architecture docs.
- **Testing frameworks and patterns to use**: No test runner or frameworks are configured.
- **Any specific testing requirements for this story**: Manual verification is sufficient for this story.
  1. Verify navigation to `/menu` displays the menu.
  2. Verify the menu displays a list of pizzas.
  3. Verify error handling displays a user-friendly message when API fails (can be simulated).
- [Source: docs/brownfield-architecture/testing-reality.md]

## Change Log

| Date       | Version | Description                 | Author             |
| ---------- | ------- | --------------------------- | ------------------ |
| 2025-12-08 | 1.0     | Initial draft for Story 1.3 | Bob (Scrum Master) |

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## QA Results
